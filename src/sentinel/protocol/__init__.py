"""
sentinel.protocol
=================
The teleportation-based Quantum Digital Signature (TQDS) protocol.

Phases: key generation -> entanglement distribution + Bell test ->
public-key teleportation -> parameter estimation -> threshold design ->
symmetrization -> signing -> verification (first recipient) -> transfer
(second recipient).
"""
